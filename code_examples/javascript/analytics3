var reportType = "11"
var reportInputFilter = {objectType: "KalturaEndUserReportInputFilter"};
reportInputFilter.categories = "Mediaspace>site>channels>podcasts";
reportInputFilter.fromDay = "20181001";
reportInputFilter.toDay = "20181031";
var pager = {objectType: "KalturaFilterPager"};
pager.pageSize = 20;
pager.pageIndex = 1;
var objectIds = "1_qwerty";

KalturaReportService.getTable(reportType, reportInputFilter, pager, objectIds)
.execute(client, function(success, results) {
  if (!success || (results && results.code && results.message)) {
    console.log('Kaltura Error', success, results);
  } else {
    console.log('Kaltura Result', results);
  }
});
